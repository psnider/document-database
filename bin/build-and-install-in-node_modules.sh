SOURCES='package.json document-database.d.ts'
TARGET=node_modules/@sabbatical/document-database
rm -fr ${TARGET}
mkdir -p ${TARGET}
cp ${SOURCES} ${TARGET}
tsc -p .
cp ./document-database.js ${TARGET} 
tsc -p tsconfig.tests.json
